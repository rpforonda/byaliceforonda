export const prerender = true; // everything static
export const csr = true;       // client-side rendering
export const ssr = false;      // no server-side rendering at runtime
export const trailingSlash = 'never';
